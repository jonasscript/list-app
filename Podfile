# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'app-list' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for app-list
  
  pod 'Alamofire'
  pod 'SDWebImage', '~> 5.0'

  target 'app-listTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'app-listUITests' do
    # Pods for testing
  end

end
